---
name: feishu-diary
description: >-
  飞书语音日记归档系统——将飞书妙记的语音日记自动提取为结构化内容（Done List、灵感与感悟、关键词、摘要），写入多维表格并追加汇总文档。
  **触发条件**：用户说"整理日记""归档日记""补充日记""记录日记""语音日记""妙记归档""今天日记"，
  或用户提到"我日记里记过""之前日记提到""查一下我X月X日的日记""帮我看看X号的日记"
  "最近日记的关键词""Done List""我的感悟"。即使只模糊提到日记/归档/Done List，
  也应触发此 skill。
  **使用时机**：用户想将妙记中的录音转为结构化日记时，或想查询/引用已归档日记时。
---

# 飞书语音日记归档

将飞书妙记（语音日记）自动化归档到多维表格和汇总文档。执行前请先完整阅读本文件，
理解全局流程后再按步骤执行。

## 依赖技能

本 skill 编排以下技能完成工作，调用前确认它们已安装：

- `lark-minutes` — 搜索妙记、获取逐字稿和 AI 产物
- `lark-base` — 读写多维表格
- `lark-doc` — 追加汇总文档

## 执行总览

```
1. 检查前置条件（飞书 CLI + Memory 配置）
2. 如果需要，走首次配置流程
3. 搜索最近妙记 + 拉取已归档记录，排重
4. 仅对新条目提取 AI 内容
5. 批量写入 Base，追加汇总文档
6. 报告本次归档结果
```

---

## 阶段 1：前置条件检查

### 1.1 检查飞书 CLI

执行 `lark-cli auth status` 检测 CLI 是否已安装并登录。

**如果命令未识别或返回错误**：
- 引用 `references/cli-install.md` 的安装步骤
- 提示用户：安装完成后重新说"整理日记"即可
- **终止流程**

**如果未登录**：
- 引导用户执行 `lark-cli auth login --recommend`
- **终止流程**

### 1.2 检查 Memory 配置

用 Glob 搜索当前项目 Memory 目录下的 `feishu-diary-config.md`：

```bash
Glob: **/feishu-diary-config.md  (在 ~/.claude/projects 下搜索)
```

或在对话上下文中检查是否已加载 `feishu-diary-config` 内容。

**如果配置不存在**：
- 阅读并执行 `references/first-run-setup.md` 中的首次配置流程
- 首次配置完成后用 Write 工具将配置写入 Memory 目录
- 重新进入主流程

**如果配置存在**：
- 从 Memory 中提取 Base Token、Table ID、汇总文档 Token、标题格式、字段映射
- 进入阶段 2

---

## 阶段 2：增量归档主流程

核心原则：**只处理新条目，不重复写入已归档内容**。

### 2.1 拉取已归档记录

从 Base 中读取**全部现有记录**，用于排重：

```bash
lark-cli base +record-list --base-token <token> --table-id <table_id> --as user --limit 200
```

如果 `has_more=true`，继续翻页直到拉取全量。

从结果中提取排重集合：
- `known_links`：所有已存在的妙记链接，用于精确排重

### 2.2 搜索妙记

用用户配置的标题格式搜索最近妙记。例如标题格式为纯日期：

```bash
lark-cli minutes +search --as user --query "<今天的日期数字>" --page-size 10 --start <30天前日期>
```

搜索范围建议覆盖最近 30 天（以防用户很久没整理），逐步收窄到最近几天。

### 2.3 排重：筛选新条目

对搜索到的每条妙记，逐条判断是否已归档：

| 判断条件 | 处理 |
|---------|------|
| 妙记链接 URL 在 `known_links` 中 | **跳过**（已归档） |
| 标题不匹配用户配置的日记格式 | **跳过**（非日记内容） |
| 以上都不满足 | ✅ **新日记，需要归档** |

**重要**：同一天可以有多条日记，即使日期和标题都相同也各自独立保留。排重仅靠妙记链接精确匹配。

如果「新条目」数量为 0，告知用户暂无新日记需要归档，终止并等待下一个任务。

### 2.4 展示待归档列表

将筛选出的新条目列出，让用户确认后再继续：

```
找到 3 条待归档日记：
  1. 妙记「6.2」— 2026-06-02 录制，时长 8 分钟
  2. 妙记「5.31」— 2026-05-31 录制，时长 12 分钟
  3. 妙记「日记5.29」— 2026-05-29 录制，时长 15 分钟

确认全部归档吗？还是跳过其中某几条？
```

如果用户说"全部"，则继续。如果用户指定某几条，只处理指定的。

---

## 阶段 3：提取内容

对每条确认的新日记，依次执行：

### 3.1 获取逐字稿

```bash
lark-cli vc +notes --minute-tokens <token> --as user
```

获取完整逐字稿文本，作为 AI 提取的原材料。

### 3.2 AI 提取四字段

**不落盘**，直接从逐字稿中提取，严格遵循 `references/output-format.md` 的格式规范。

提取前先阅读 `references/output-format.md`。

**逐字稿摘要**（一句话）：
- 提取最核心的 3-6 件事
- 用分号分隔
- 省略主语（默认"我"）
- 不要超过 100 字

**Done List**（✅ 前缀列表）：
- 提取所有「完成了什么」的动作
- 每条必须有具体对象（项目名、人名、工具名）
- 从逐字稿中识别：做了什么、完成了什么、去了哪里、和谁一起
- **合并同类项**：同一件事在日记中多次提及，只记录一条，把重复描述合并为最精炼的一条
- 每条一行

**灵感与感悟**（💡 前缀列表）：
- 提取所有反思、洞察、感悟
- **尽量摘录逐字稿中的原话**，只做必要的删减（去掉口头禅、重复语气词），不过度概括或改写
- 保留用户的因果关系推理（"因为……所以……"）和口语风格
- 如果逐字稿中没有明确的感悟，可以写"今日无特别感悟"或留空

**关键词**（3-5 个，逗号分隔）：
- 涵盖核心主题
- 包含具体工具/平台/人名
- 不超过 10 个

---

## 阶段 4：写入

### 4.1 写入 Base

使用 `+record-batch-create`（**不是 upsert**），确保同一天多条日记各自独立。

格式为 `{"fields":["字段名1","字段名2",...], "rows":[["值1","值2",...]]}`，`fields` 和 `rows` 必须按相同列顺序一一对应：

```bash
lark-cli base +record-batch-create --base-token <token> --table-id <table_id> --as user \
  --json '{"fields":["日期","标题","妙记链接","逐字稿摘要","Done List","灵感与感悟","关键词"],"rows":[["<YYYY-MM-DD HH:mm:ss>","<标题>","<妙记URL>","<摘要>","<Done List>","<灵感>","<关键词>"]]}'
```

多行时在 `rows` 数组中追加即可，单次最多 200 行。

**注意**：
- 只写入实际存在于 Base 中的字段名（参考 Memory 中的字段映射）
- 妙记链接字段可点击查看逐字稿，无需额外写入逐字稿链接字段
- 如果某条记录写入失败，报告具体错误但继续处理下一条
- 批量单次不超过 200 条；超过时分批，批次间延迟 0.5-1 秒

### 4.2 追加汇总文档

**Done List 汇总**——追加到 Done List 汇总文档末尾：

```
**<标题>**
<Done List 内容>
（末尾保留一个空行）
```

**灵感与感悟汇总**——追加到灵感与感悟汇总文档末尾：

```
**<标题>**
<灵感与感悟内容>
（末尾保留一个空行）
```

追加时使用 `--command str_replace`，将第一个已有标题替换为"新条目 + 原标题"，实现倒序插入（最新在最前）：

```bash
# 1. 将新内容写入临时文件（Markdown 格式，参照 output-format.md）
#    文件内容示例：
#    ## 6.2 · 2026-06-02
#    - Done List 条目一
#    - Done List 条目二
#
#    ## 6.1 · 2026-06-01
#
# 2. cd 到临时文件所在目录
# 3. 用 str_replace 替换第一个标题，实现新条目插在顶部：
lark-cli docs +update --api-version v2 --as user --doc <汇总文档token> \
  --command str_replace --pattern "## <第一个已有标题>" --content @<相对路径>.md
```

**关键点**：
- `--content` 必须是相对路径（不支持绝对路径）
- `--pattern` 匹配的是文档的 Markdown 源文本（如 `## 6.1 · 2026-06-01`）
- 替换内容末尾必须包含原标题（`## 原标題`），否则原标题会丢失
- 如果 `str_replace` 产生格式问题（如纯文本"6.1"变成 `<p>` 而非 `<h2>`），再用一次 `str_replace --pattern "纯文本" --content "## 纯文本"` 修复

两个汇总文档分别操作，格式参见 `references/output-format.md`。

### 4.3 报告结果

```
本次归档完成：
  ✅ 6.2 → Base 已写入 + 两个汇总文档已追加
  ✅ 5.31 → Base 已写入 + 两个汇总文档已追加
  ⏭️ 5.29（日记5.29）→ 已跳过（用户指定）
```

---

## 日常对话中的日记引用

当用户在非归档场景中提到日记相关内容时，skill 的 description 会自动触发。

**前提**：`feishu-diary-config` 已在 MEMORY.md 中注册，会话启动时由 Memory 系统自动加载到上下文。

**示例场景**：
- 「我上次日记里提到的那个面试怎么样了」
- 「查一下我关于小红书的感悟」
- 「我 5 月的关键词有哪些」

**执行方式**：
1. skill 被触发后，从已加载的 Memory 上下文读取 Base Token、字段映射
2. 用 `lark-cli base +record-search` 按关键词搜索，或按日期筛选
3. 读取匹配记录的 Done List / 灵感与感悟 / 摘要字段
4. 结合用户当前问题，做上下文关联回复

**实现原理**：
- 首次配置时，`first-run-setup.md` 引导 AI 将 `feishu-diary-config.md` 写入 Memory 目录，并在 `MEMORY.md` 中注册引用
- 后续每次新会话，Claude Code 自动加载 `MEMORY.md` 中列出的所有 memory 文件
- 用户提到日记时，skill 被触发，直接从当前上下文中读取配置，无需用户重复说明

---

## 注意事项

- **每步验证**：搜索妙记后检查返回是否为空；提取内容后检查格式是否合规；写入后检查返回是否成功。
- **只做增量**：排重是核心，宁可多判一条「已归档」也不要重复写入。
- **用户确认**：待归档列表中涉及 10 条以上时，先展示摘要再让用户确认；10 条以内直接走批量流程，仅汇报最终结果。
- **部分失败处理**：如果某条写入失败，报告详情但继续处理其余条。最后汇总成功/失败数量。
- **日期格式**：写入 Base 的日期字段必须使用 `YYYY-MM-DD HH:mm:ss` 格式（如 `2026-06-02 08:30:00`），妙记创建时间可从 `minutes +get` 返回中提取。
